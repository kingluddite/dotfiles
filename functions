gulp-boilerplate(){
  git clone https://github.com/kingluddite/webpack-gulp-boilerplate.git $1 && cd $1 && yarn && npm run dev
}

eslint-boilerplate(){
  git clone https://github.com/kingluddite/eslint-boilerplate.git $1 && cd $1 && yarn
}
